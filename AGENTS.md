# AGENTS.md

## 목적

이 문서는 코딩 스타일 문서가 아니라, `Front-end` 리포지토리에서 작업하는 팀원별 AI 에이전트의 판단 기준을 통일하기 위한 얼라인먼트 문서이다. 에이전트는 단순히 코드를 생성하는 도구가 아니라, 제품 방향성과 전체 시스템 일관성을 지키는 프론트엔드 협업자로 동작해야 한다.

이 문서는 사람용 설명서가 아니다. 에이전트는 현재 리포지토리의 설정, 의존성, 파일 구조를 직접 확인하고 그에 맞춰 작업한다.

## 제품 방향성

국민클로 프론트엔드는 사용자가 학교 공지, 추천 정보, 개인화된 할 일을 빠르게 이해하고 행동할 수 있게 돕는 화면이어야 한다. 핵심 문제는 "정보와 탐색할 채널이 많아서 필요한 공지를 놓치는 것"이며, 프론트엔드는 이 문제를 더 복잡하게 만들지 않고 탐색, 이해, 저장, 확인 흐름을 단순하게 만들어야 한다.

에이전트는 개별 작업을 잘 끝내는 것보다 팀 전체 결과물이 일관되게 유지되는 것을 최우선으로 삼는다.

- 기능 추가보다 실제 문제 해결을 우선한다.
- 개별 화면의 완성도보다 전체 사용자 흐름의 일관성을 우선한다.
- 기술적으로 멋진 구현보다 팀이 이해하고 유지할 수 있는 구현을 우선한다.
- 빠른 구현이 필요해도 이후 팀원이 이해할 수 없는 코드는 만들지 않는다.
- 안전, 개인정보, 권한, 제출 방지 같은 보호 로직은 AI 판단이나 추론보다 보수적이고 결정론적인 조건과 명시적 검증을 우선한다.

## 작업 범위

프론트엔드 리포지토리에서는 다음 범위의 작업만 직접 수행한다.

- 화면, 레이아웃, 컴포넌트, 스타일
- 클라이언트 라우팅과 사용자 플로우
- 브라우저 상태, 폼 상태, UI 피드백
- API 연동을 위한 클라이언트 요청 코드
- 프론트엔드 빌드와 배포에 영향을 주는 설정

다른 영역의 결정이 필요하면 필요한 계약, 예시 payload, 막힌 지점, 선택지를 정리하고 사용자에게 확인한다.

## 구현 전 확인

에이전트는 구현을 시작하기 전에 다음을 먼저 파악한다.

- 작업의 목적과 해결하려는 문제
- 관련 파일, 화면, 컴포넌트, 라우트
- 현재 구조에서 최소로 바꿀 수 있는 범위
- 기존 아키텍처와의 관계
- 예상 리스크와 검증 방법

목적이 불분명하면 기능을 먼저 만들지 않는다. 필요한 문제 정의, 정책, API 계약, 화면 흐름을 확인한 뒤 진행한다.

## 작업 원칙

- 변경 전 현재 화면 구조, 라우팅, 컴포넌트 책임을 먼저 파악하여 기존 아키텍처와 코드 구조를 존중한다.
- 구조 변경이 필요하면 변경 이유, 영향 범위, 대안을 설명하고 최소 범위로 수행한다.
- 명백한 사용자 경험 문제나 회귀 가능성이 있는 경우에만 근거를 설명하고 기존 코드 스타일을 변경한다.
- 요청 범위를 벗어난 리팩터링, 폴더 재배치, 스타일 체계 변경은 하지 않는다.
- 미검증 최적화를 적용하지 않고, 새 추상화는 실제 중복이나 복잡도를 줄일 때만 만든다.
- 기존 동작을 깨뜨릴 수 있는 변경은 반드시 영향 범위를 함께 확인한다.
- 임시 구현, mock 데이터, 하드코딩 값은 실제 사용자 플로우에 노출되지 않게 하고 쉽게 제거할 수 있게 분리한다.
- 사용자 입력값, 학번, 이메일, 토큰, API 응답 전체 payload, 디버그 로그는 화면이나 console에 그대로 노출하지 않는다.
- 저장, 삭제, 제출, 알림 설정처럼 사용자의 상태를 바꾸는 동작은 로딩, 성공, 실패, 중복 제출 방지 상태를 갖춰야 한다.
- 에러는 조용히 삼키지 않고, 사용자가 이해할 수 있는 메시지 또는 회복 가능한 흐름을 제공한다.
- 새 의존성은 꼭 필요한 경우에만 추가하고, 이미 있는 라이브러리나 브라우저 기본 기능으로 충분한지 먼저 확인한다.
- 이모지는 UI에서 최대한 지양한다. 아이콘이 필요하면 프로젝트의 기존 아이콘 방식과 맞춘다.
- dead code, 주석 처리된 버튼, 디버그 출력, 미사용 함수는 PR에 남기지 않는다.
- 파일명, 컴포넌트명, export 방식, 도메인 용어는 기존 관례와 일관되게 유지한다.
- 파일, 모듈, 컴포넌트는 책임 단위가 명확해야 하며 거대한 단일 파일, 중복 상태, 숨은 의존성, 매직 넘버를 피한다.

## UI 기준

UI는 "보이는 것"이 아니라 "사용자가 헷갈리지 않고 끝까지 행동할 수 있는 흐름"으로 검토한다.

- 첫 화면은 실제 사용 가능한 화면이어야 한다. 안내문만 있는 랜딩 페이지로 대체하지 않는다.
- 네비게이션은 빈 본문, 깨진 레이아웃, 미완성 화면으로 이어지면 안 된다. 준비되지 않은 메뉴는 비활성화, 준비중 상태, 기존 안전한 경로 중 하나로 처리한다.
- 버튼처럼 보이는 요소는 실제로 클릭 가능해야 하며, 링크 이동은 링크로, 동작 실행은 버튼으로 구현한다.
- 폼에는 필수값, 입력 형식, 오류 메시지, 저장 중, 완료 상태가 명확히 보여야 하며 사용자가 수정해서 회복할 수 있어야 한다.
- 성공 화면은 다음 행동을 제시하고, 오류 화면은 재시도, 이전 화면, 안전한 기본 화면 중 하나를 제공한다.
- 모바일과 데스크톱에서 텍스트, 버튼, 카드, 사이드바가 겹치거나 잘리면 안 된다. 긴 텍스트, 긴 학과명, 긴 공지 제목, 빈 데이터 상태를 고려한다.
- 색상만으로 상태를 구분하지 않고 텍스트, 아이콘, 배지, 위치 등 보조 신호를 함께 둔다.
- 모달, 드롭다운, 자동완성 목록은 화면 밖으로 잘리지 않게 하고, hover에만 의존하지 않는다.
- 반복되는 카드나 목록은 장식보다 정보 우선순위와 스캔 가능성을 우선한다.
- 기존 화면의 밀도와 톤을 유지한다. 과장된 애니메이션이나 효과는 사용하지 않는다.

## 라우팅과 플로우

- 새 경로를 추가하면 진입 경로, 직접 URL 접근, 새로고침, 뒤로가기를 함께 확인한다.
- 사이드바, 헤더, CTA 버튼의 이동 경로는 실제 라우트 정의와 일치해야 한다.
- 존재하지 않는 경로는 명확한 fallback을 둔다.
- 페이지 전환 후에도 공통 레이아웃이 유지되는지 확인한다.
- 특정 페이지에서 공통 UI를 숨길 때는 이유가 명확해야 하며, 다른 페이지에 부작용이 없어야 한다.

## 상태와 데이터

- 서버 계약이 확정되지 않은 mock data를 사용할 경우 임시임을 코드와 UI에서 분명히 드러낸다.
- API 요청/응답 형태는 한 곳에서 바꾸기 쉽게 구성한다.
- 저장 실패 후 사용자가 입력한 값이 사라지면 안 된다.
- 빈 목록, 검색 결과 없음, 권한 없음, 네트워크 실패 상태를 구분한다.
- 브라우저 전역 객체에 업무 데이터를 저장하지 않는다. 데모 목적이라도 UI에 노출하지 않는다.

## 검증 기준

작업 후에는 변경 범위에 맞는 가장 작은 검증을 수행한다. UI 변경이라면 빌드 통과만으로 충분하다고 보지 않는다.

기본 확인 항목:

- 빌드가 통과하는지
- 추가한 import와 의존성이 실제 설치/빌드 환경에서 해결되는지
- 변경한 화면에 직접 진입할 수 있는지
- 주요 클릭 경로가 빈 화면으로 이어지지 않는지
- 폼의 정상/오류/로딩/완료 상태가 동작하는지
- 모바일 폭에서 텍스트와 컨트롤이 겹치지 않는지
- console에 민감 정보나 불필요한 디버그 로그가 남지 않는지

리뷰만 수행할 때는 실제 사용자에게 영향을 주는 문제를 우선한다. 스타일 취향보다 회귀, 접근성, 데이터 노출, 라우팅 오류, 상태 손실, 검증 누락을 먼저 본다.

## PR 및 완료 보고

PR에는 다음을 포함한다.

- 사용자가 보게 되는 변경 요약
- 변경 이유
- 주요 화면 또는 플로우
- 검증한 명령과 수동 확인 내용
- 아직 mock이거나 후속 연동이 필요한 부분
- 남은 리스크나 다른 리포지토리/팀 결정이 필요한 부분

PR에 포함하지 않는다.

- 디버그용 화면 출력
- console 로그
- 사용하지 않는 코드
- 임시 경로
- 설명 없는 대규모 스타일 변경
- 요청 범위 밖 리팩터링
